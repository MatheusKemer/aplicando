<%= render "teacher_question", question: @question%>

<p><%= l @question.created_at %>

<p><%= @question.discipline.name %>
<br>
<br>

<%= link_to I18n.t("menu.edit"), edit_question_path(@question) %>

<br>

<%= link_to I18n.t("menu.back"), questions_path %>

<br>

<%= link_to I18n.t("menu.destroy"), @question, method: :delete, data: { confirm: 'Are you sure?' } %>
