<p id="notice"><%= notice %></p>

<h1>Dones</h1>

<table>
  <thead>
    <tr>
      <th>Prova</th>
      <th>Nota</th>
      <th>Respostas</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @dones.each do |done| %>
      <tr>
        <td><%= link_to Exam.where(id: done.exam_id).first.title, Exam.where(id: done.exam_id).first %></td>
        <td><%= done.nota%></td>
        <td><%= done.respostas %></td>
        <td><%= link_to 'Show', done %></td>
        <td><%= link_to 'Edit', edit_done_path(done) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Done', new_done_path %>
